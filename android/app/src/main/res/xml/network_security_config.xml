<?xml version="1.0" encoding="utf-8"?>
<!-- TARDIS has no app-layer authentication, so the shell only talks plain HTTP
     to the device itself (a server running on the phone, or the emulator's host
     alias). Everything else must be HTTPS, normally Tailscale Serve. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
